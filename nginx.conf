server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;

    # SPA: cualquier ruta que no sea un archivo estatico existente cae en
    # index.html para que react-router-dom maneje el routing del lado cliente.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Assets versionados por hash (dist/assets/*) — cache largo y agresivo.
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
